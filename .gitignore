# 編輯器 / 工具的本機狀態，與專案無關
.claude/
.vscode/
.idea/
*.swp
*~

# 腳本執行期產物（正常位置是 /var/log/OPS-ssh，這裡只擋誤放進 repo 的情況）
*.log
OPS-ssh/
n9e-selfheal/

# 壓測產物：報告落在「執行時所在目錄」底下的 logs/，從 repo 目錄跑就會生在這裡。
# fio 測試檔正常會被 trap 清掉，.fio-test.* 這行是為了 kill -9 之後的殘骸——
# 那可能是個 4GB 的檔案，絕對不能不小心 commit 進來。
logs/
.fio-test.*
